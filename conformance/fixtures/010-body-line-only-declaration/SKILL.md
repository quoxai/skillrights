---
name: readme-header-only
description: Carries the human-readable companion line but no frontmatter declaration.
---

# readme-header-only

SkillRights-NoTrain-1.0: AI execution permitted. Model training rights reserved. https://skillrights.org/notrain/1.0

Body text. Per SPEC.md's "The declaration" section, the machine-readable
declaration is the frontmatter `license:` field; the plain-text line above
is a companion for human readers, crawlers and corpus filters, "added
near the top of the file body or README" IN ADDITION to the frontmatter
field, not instead of it. This file has the companion line but no
frontmatter `license:` field, so per spec it carries no declaration an
installer can act on.
